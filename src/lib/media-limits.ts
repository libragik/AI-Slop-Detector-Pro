// Product limits shared by browser validation and the server. MB is decimal,
// matching the Sightengine Pro plan's 500 MB allowance.
export const MAX_VIDEO_BYTES = 500_000_000;
export const MAX_VIDEO_SECONDS = 180;
